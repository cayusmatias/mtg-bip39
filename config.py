# The oder os sets is important, because the offset is calculated based on the index of the set in the list.
# A diferent order generate a diferent card for the same word.
SET_CODES = ['mkm', 'lci', 'woe', 'mom', 'one', 'bro', 'dmu', 'snc']
